/**
 * Global test setup.
 *
 * Deliberately empty. The correctness suite must exercise the real modules with
 * as little scaffolding between the test and its subject as possible — any
 * global mock here would be a place for a correctness bug to hide.
 */

export {};
