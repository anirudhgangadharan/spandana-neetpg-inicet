services:
  - type: web
    name: spandana-neetpg-inicet
    runtime: docker
    dockerfilePath: ./Dockerfile.fetch
    plan: free
    region: singapore
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
